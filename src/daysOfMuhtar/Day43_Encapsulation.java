package daysOfMuhtar;

public class Day43_Encapsulation {
    /**
     * Encapsulation:
     * 1.privita access modifier
     * getter and setter
     * multi-class
     * class:
     * data/attribute==>instance variables
     * Actions==>instance method
     * Encapsulations:
     *to hide the instance variable(hiding the data)
     * instance variable:belongs to the object.each object has its
     */

}
